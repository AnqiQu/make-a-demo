{
  "scriptId": "barebones-chat-sample-playwright-001",
  "title": "Barebones Chat Demo Script",
  "version": 1,
  "estimatedDurationSeconds": 20,
  "format": "16:9",
  "sections": [
    {
      "id": "section-001",
      "title": "Start a Chat",
      "scenes": [
        {
          "id": "scene-001",
          "humanReadableDescription": "Show the empty chat app, click the new chat button, and confirm a fresh conversation is ready.",
          "durationSeconds": 5,
          "events": [
            "Navigate to the chat app.",
            "Click the New chat button.",
            "Wait for the chat status to show that a new chat is ready.",
            "Confirm the previous conversation history is cleared.",
            "Click Launch Plan Chat.",
            "Confirm the saved conversation history is restored.",
            "Pause on the restored chat."
          ],
          "playwrightScript": "import { chromium, expect } from '@playwright/test';\n\nconst browser = await chromium.launch();\nconst context = await browser.newContext({\n  viewport: { width: 1280, height: 720 },\n  recordVideo: {\n    dir: 'artifacts/videos',\n    size: { width: 1280, height: 720 }\n  }\n});\n\nconst page = await context.newPage();\nconst pause = () => page.waitForTimeout(500);\nawait page.goto('http://localhost:3000');\nawait pause();\nawait expect(page.getByRole('heading', { name: /barebones chat/i })).toBeVisible();\nawait expect(page.getByText(/welcome to the barebones chat demo/i)).toBeVisible();\nawait page.getByRole('button', { name: /new chat/i }).click();\nawait pause();\nawait expect(page.getByText(/new chat ready/i)).toBeVisible();\nawait expect(page.getByText(/welcome to the barebones chat demo/i)).not.toBeVisible();\nawait expect(page.getByText(/no messages yet/i)).toBeVisible();\nawait page.getByRole('button', { name: /launch plan chat/i }).click();\nawait pause();\nawait expect(page.getByText(/saved chat loaded/i)).toBeVisible();\nawait expect(page.getByText(/welcome to the barebones chat demo/i)).toBeVisible();\nawait expect(page.getByLabel(/message/i)).toBeVisible();\nawait page.waitForTimeout(2500);\n\nawait context.close();\nawait browser.close();"
        },
        {
          "id": "scene-002",
          "humanReadableDescription": "Type a message into the chat box, send it, and show the deterministic assistant response.",
          "durationSeconds": 6,
          "events": [
            "Click the message input.",
            "Type a short user message.",
            "Click Send.",
            "Wait for the user message to appear.",
            "Wait for the assistant response to appear.",
            "Confirm the transcript scrolled to the bottom."
          ],
          "playwrightScript": "import { chromium, expect } from '@playwright/test';\n\nconst browser = await chromium.launch();\nconst context = await browser.newContext({\n  viewport: { width: 1280, height: 720 },\n  recordVideo: {\n    dir: 'artifacts/videos',\n    size: { width: 1280, height: 720 }\n  }\n});\n\nconst page = await context.newPage();\nconst pause = () => page.waitForTimeout(500);\nawait page.goto('http://localhost:3000');\nawait pause();\nawait page.getByRole('button', { name: /new chat/i }).click();\nawait pause();\nawait page.getByLabel(/message/i).click();\nawait pause();\nawait page.getByLabel(/message/i).fill('Show me the launch plan');\nawait pause();\nawait page.getByRole('button', { name: /send/i }).click();\nawait pause();\nawait expect(page.getByText('Show me the launch plan')).toBeVisible();\nawait expect(page.getByText(/demo reply: here is a simple launch plan/i)).toBeVisible();\nconst transcript = page.getByRole('log', { name: /conversation transcript/i });\nawait expect.poll(async () => transcript.evaluate((element) => element.scrollTop + element.clientHeight >= element.scrollHeight)).toBe(true);\nawait page.waitForTimeout(2500);\n\nawait context.close();\nawait browser.close();"
        }
      ]
    },
    {
      "id": "section-002",
      "title": "Review Chat Details",
      "scenes": [
        {
          "id": "scene-003",
          "humanReadableDescription": "Hover over the archived chat item to reveal its action hint and show the app has saved conversations.",
          "durationSeconds": 4,
          "events": [
            "Navigate to the chat app.",
            "Hover over the archived Launch Plan chat item.",
            "Wait for the hover action hint to appear.",
            "Pause on the highlighted chat item."
          ],
          "playwrightScript": "import { chromium, expect } from '@playwright/test';\n\nconst browser = await chromium.launch();\nconst context = await browser.newContext({\n  viewport: { width: 1280, height: 720 },\n  recordVideo: {\n    dir: 'artifacts/videos',\n    size: { width: 1280, height: 720 }\n  }\n});\n\nconst page = await context.newPage();\nconst pause = () => page.waitForTimeout(500);\nawait page.goto('http://localhost:3000');\nawait pause();\nawait page.getByRole('button', { name: /launch plan chat/i }).hover();\nawait pause();\nawait expect(page.getByText(/open saved launch plan/i)).toBeVisible();\nawait page.waitForTimeout(2500);\n\nawait context.close();\nawait browser.close();"
        },
        {
          "id": "scene-004",
          "humanReadableDescription": "Scroll through the conversation transcript and show the older and newer messages in the same chat.",
          "durationSeconds": 5,
          "events": [
            "Navigate to the chat app.",
            "Scroll down inside the message transcript.",
            "Wait for the final checklist message to be visible.",
            "Scroll back to the top of the message transcript.",
            "Wait for the first welcome message to be visible."
          ],
          "playwrightScript": "import { chromium, expect } from '@playwright/test';\n\nconst browser = await chromium.launch();\nconst context = await browser.newContext({\n  viewport: { width: 1280, height: 720 },\n  recordVideo: {\n    dir: 'artifacts/videos',\n    size: { width: 1280, height: 720 }\n  }\n});\n\nconst page = await context.newPage();\nconst pause = () => page.waitForTimeout(500);\nawait page.goto('http://localhost:3000');\nawait pause();\nconst transcript = page.getByRole('log', { name: /conversation transcript/i });\nawait transcript.evaluate((element) => { element.scrollTop = element.scrollHeight; });\nawait pause();\nawait expect(page.getByText(/final checklist: validate, script, capture, compose/i)).toBeVisible();\nawait transcript.evaluate((element) => { element.scrollTop = 0; });\nawait pause();\nawait expect(page.getByText(/welcome to the barebones chat demo/i)).toBeVisible();\nawait page.waitForTimeout(2500);\n\nawait context.close();\nawait browser.close();"
        }
      ]
    }
  ]
}

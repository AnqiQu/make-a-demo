# Docs Instructions

`SEED_TEMPLATE_README.md` is a legacy record from the code template imported into this repository as a starter. Do not rename it, rebrand it to MakeADemo, or update its template-specific wording unless the user explicitly asks to edit the legacy template record itself.

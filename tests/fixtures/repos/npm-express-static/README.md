# Trailhead

Plan a day hike: convert distances between miles and kilometers, and keep a packing
checklist. Static client built into `dist/` and served with `serve`; a small express
API companion lives in `server.mjs`.

MakeADemo pipeline fixture — build-before-serve detection (see `../README.md`).

```
npm install
npm run build
npm start
```

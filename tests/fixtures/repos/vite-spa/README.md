# Fieldnote

A single-page notes app for outdoor work: add notes, search them, and pin the important
ones to the top.

MakeADemo pipeline fixture — single-package Vite SPA (see `../README.md`).

```
npm install
npm run dev
```
